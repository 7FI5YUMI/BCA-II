#include<stdio.h>
int main()
{
	int n;
	float m;
	char ch;
    	printf("The size of Integer = %d byte",sizeof(n));
    	printf("\nThe size of Float = %d byte",sizeof(m));
    	printf("\nThe size of Characterr = %d byte",sizeof(ch));
	return 0;
}
